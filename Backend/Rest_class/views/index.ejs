<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link rel="stylesheet" href="/index.css"/>
</head>
<body>
    <h1>Quora posts</h1>
    <% for(let post of posts){%>
        <div class = "post">
            <h3 class = "user">@ <%= post.name %> </h3>
            <h3 class = "content"><%= post.content %></h3>
            <a href="http://localhost:8080/posts/<%= post.id %>">See in detail</a> <br>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a> <br>
            <form method = "post" action="/posts/<%=post.id%>?_method=DELETE">
                        <button>delete</button>
            </form>
        </div>
    <%} %>
    <a href="http://localhost:8080/posts/new">Create new post</a>
</body>
</html>